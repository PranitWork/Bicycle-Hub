{
  "name": "backend",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "start": "npx json-server db.json",
    "build": "echo 'No build step needed'"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}